export const companyPlanEventNames = {
  /** Evento disparado quando a quantidade ativa de membros mudar. */
  MEMBERS_COUNT_UPDATED: 'company-plan-members-count-updated',
  /** Evento disparado quando a quantidade de convites pendentes mudar. */
  INVITES_COUNT_UPDATED: 'company-plan-invites-count-updated',
  /** Evento disparado quando a quantidade ativa de pacientes mudar. */
  PATIENTS_COUNT_UPDATED: 'company-plan-patients-count-updated',
}
